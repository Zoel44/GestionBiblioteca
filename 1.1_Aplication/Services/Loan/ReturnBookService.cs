using GestionBiblioteca.Domain.Entities;
using GestionBiblioteca.Domain.Interfaces;
using GestionBiblioteca.Aplication.DTO;

namespace GestionBiblioteca.Aplication.Services
{
    public class ReturnBookService
    {
       /* IRepositoryLoans repo;
        public ReturnBookService(IRepositoryLoans p_repo)
        {
           repo = p_repo; 
        }
        public void ejecutar(string p_ID)
        {
            Loan? loan = repo.searchLoanForID(p_ID);
            if(loan == null)
            {
                throw new Exception ("No existe el libro");
            }
            else
            {
                repo.returnLoan(p_ID);
            }

        }*/
    }
}